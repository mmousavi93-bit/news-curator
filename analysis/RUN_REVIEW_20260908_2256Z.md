# Run review — 2026-09-08 22:56Z (02:26 Tehran, 1405/06/18)

Artifacts: `outputs/run|chosen|summaries|read_20260908T225636Z.csv`, `outputs/flash_2026090[89]*.csv`.
First live escalation-night review since 9q/9r. Owner pasted the two delivered digests + 4 flash alerts.

## Headline verdict

9q + 9r are **verified working**. The blackout is closed:

| | 9q run (2026-09-06 22:12) | this run |
|---|---|---|
| items | 905 | 474 |
| delivered events | 2 | 19 |
| messages | 1 | 2 |

Expected band from CLAUDE.md for an escalation run was "~14–20 across 2 messages". Measured 19 / 2.
Do not re-tune `event_repeat_threshold` or `min_score` downward on this evidence — both held.

**The new failure mode is the opposite of the old one: redundancy, not silence.**

## Defect 1 — one event ships as four (BLOCKING for readability)

`chosen.csv`, same run:

Tanker strike:
- n=48, ind=8 — «سنتکام پنج نفتکش ایرانی را در خلیج فارس و دریای عمان منهدم کرد» → SENT
- n=12, ind=3 — «ارتش آمریکا پنج نفتکش مرتبط با ایران را هدف قرار داد و منهدم کرد» → dropped, `same_run_dup sim=0.83`
- n=9,  ind=2 — «سنتکام پنج نفتکش ایرانی را پس از حمله موشکی به ناو آمریکایی منهدم کرد» → SENT
- n=3,  ind=1 — «پنتاگون اعلام کرد پنج کشتی نفتکش ایرانی را غیرفعال کرد» → SENT

Azraq / Jordan missile attack:
- n=59, n=18, n=23, n=4 → **all four SENT** as separate entries.

So ~7 of 19 delivered entries are re-tellings of 2 stories. The 0.80 same-run gate fired exactly
once, at 0.83. Everything else sat below it despite near-identical headlines.

**This is measured evidence against the 9r `UNVERIFIED` note.** But the fix is NOT lowering 0.80 —
that re-opens the 9q strike/retaliation collapse (those legitimately sit 0.60–0.65). Root cause is
upstream: 474 items → 128 clusters, and the top story fragmented into 4 clusters totalling 72
members. **Fix the clusterer, not the gate.**

Investigate, in order:
1. What text `samerun_dedup` embeds — headline only, or headline+summary? Two LLM paraphrases of the
   same wire story diverge fast in summary prose; the headline pair above should be >0.9 alone.
2. Whether `pipeline/cluster.py`'s greedy single-pass leaves orphan clusters that a second merge
   pass would absorb (cheap, local, zero LLM).
3. Only then consider an entity/number-overlap tiebreak ("پنج نفتکش" + "سنتکام"/"پنتاگون") as a
   deterministic same-story override at MID sim. Not an LLM call.

## Defect 2 — «پیگیری» fires on 16 of 19 entries (MAJOR)

`fate` counts: `sent_followup` 16, `sent` 3. A label carried by 84% of output carries no
information, and it is factually wrong on brand-new events: Iran's first missile strike on Jordanian
bases is tagged as a follow-up (sim 0.66 to a prior tanker cluster).

MID-band membership is being rendered as "پیگیری". MID band means *different story* per the 9q
design — it should render as a normal entry with no follow-up marker. Only HIGH-band bypass
survivors should carry «پیگیری».

## Defect 3 — LLM cascade failed 60% of calls on the biggest news night (BLOCKING)

`run.csv`: `calls_bai=6 fails_bai=4 | calls_gemini=4 fails_gemini=3 | calls_groq=60 fails_groq=35`
Total 70/70 — the `max_calls_per_run` cap was **exhausted**. 42 of 70 attempts failed.

Consequence in `chosen.csv`: `unavailable` 7 + `cap_refused` 6 = **13 of 40 kept clusters produced
no summary**, which is why the ugly `⚠️ خلاصه خودکار در دسترس نیست — عناوین خام منابع:` fallback
appeared at the bottom of **both** delivered messages, dumping untranslated English/Arabic headlines
into a Persian digest.

- Gemini is effectively dead: 4 calls, 3 fails. Its real quota is **20 RPD** (corrected 2026-09-05).
  40 clusters × 6 runs = ~240 calls/day needed. Gemini covers ~8% of one run's demand, once a day.
  **The CLAUDE.md line "~288 calls/day, 5x margin" is stale — it was written against the old
  1,500 RPD figure and should be struck.**
- Groq is now the de-facto primary and failed 35/60. Cause is not in the CSVs — it is in the
  workflow log. Get it before changing anything: 429 pacing (30 RPM vs 60 calls fired in one run)
  and model-side refusals look different and need different fixes.

This is the highest-value fix on the board. Everything else is cosmetic next to 1/3 of clusters
silently producing nothing during a shooting war.

## Defect 4 — flash alerts firing on shitposts and mis-parsed locations (MAJOR)

Four alerts overnight. At least two were non-events:

- 20:59 — «مجتبی خامنه ای چیست؟ … کانال توییت مردمی» = a Telegram channel self-promo/opinion post.
  Location rendered **📍 خامنه ای** — a person's name parsed as a place.
- 02:07 — «یعنی میشه یکیش بخوره اسرائیل؟» = a user speculating out loud. Promoted to 🚨.
- 04:05 — location **📍 پاسداران**, extracted from «روابط عمومی سپاه پاسداران» (IRGC public
  relations). Confirmed in the CSVs: token `پاسداران` → ring `city` (2 hits). Pasdaran *is* a Tehran
  district, but it collides with «سپاه پاسداران», which appears in nearly every military message.
  **Guaranteed false-positive generator. Needs a negative-context rule: `پاسداران` preceded by
  `سپاه` is never a location.**
- Alert text truncates mid-word («…که قطعا جوابش ق»).

Source concentration across 317 flash rows (4 CSVs): `tg_iranian_militarism` 66, `tg_tabzlive` 55,
`tg_geopwatch` 46, `tg_wfwitness` 45. 3 of 4 delivered alerts came from `tg_tweet_mardomi` **alone**,
uncorroborated. Kind breakdown: `tehran:no_location` 118 + `escalation:no_location` 68 = 186 of 317
rows term-matched with no location at all; `stale` 38 (the known tg_wfwitness/tg_tabzlive issue).

## Defect 5 — one pipeline run's artifacts are missing

Only the 22:56Z set is present. The 21:33 Tehran (17:33Z→18:03Z) digest was delivered — the owner
pasted it — but its `chosen/run/summaries` CSVs are not in `outputs/`. Either the artifact was not
downloaded, or the run did not upload one. Check Actions before assuming the former.

## Do next, in this order

1. Get the Groq failure reason from the workflow log. Do not touch config first.
2. Fix cluster fragmentation (Defect 1) — upstream, deterministic, zero LLM cost.
3. Fix the «پیگیری» label condition (Defect 2) — one-line render condition.
4. Flash: negative-context rule for `پاسداران`, and require ≥2 sources OR a tier-2 source before a
   `tg_tweet_mardomi`-only message ships.
5. Only after 2–3 clean runs: revisit `event_repeat_threshold` and `min_score`. Not before.

**Do not change more than one variable per run.**
